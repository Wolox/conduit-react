const paths = {
  home: '/',
  login: '/login',
  recoverPassword: '/recover_password',
  register: '/register',
  editor: '/editor',
  editorBySlug: '/editor/:slug',
  settings: '/settings',
  profile: '/profile',
  article: '/article/:slug',
  user: '/user/:username',
  errorScreen: '/error'
};

export default paths;
