const paths = {
  home: '/',
  login: '/login',
  recoverPassword: '/recover_password',
  register: '/register',
  editor: '/editor',
  settings: '/settings',
  profile: '/profile'
};

export default paths;
